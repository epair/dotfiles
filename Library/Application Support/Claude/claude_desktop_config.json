{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "$HOME/code/dotfiles",
        "$HOME/personal/brain"
      ]
    }
  }
}
